<?php
$conn = new mysqli("localhost", "root", "", "ePKAH");
if ($conn->connect_error) {
    die("Connection failed: " . $conn->connect_error);
}

$email = $_GET['email'] ?? '';
$sql = "SELECT * FROM users WHERE email = ?";
$stmt = $conn->prepare($sql);
$stmt->bind_param("s", $email);
$stmt->execute();
$result = $stmt->get_result();
$user = $result->fetch_assoc();

if (!$user || $user['peranan'] !== 'vendor') {
    echo "Akses tidak sah atau vendor tidak dijumpai.";
    exit;
}
?>

<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Profil Vendor</title>
    <style>
        body {
            font-family: 'Segoe UI', sans-serif;
            background: #f0fff0;
            padding: 20px;
        }
        .profile-card {
            max-width: 600px;
            margin: auto;
            background: #e8f5e9;
            padding: 25px;
            border-radius: 15px;
            box-shadow: 0 4px 8px rgba(0,0,0,0.15);
        }
        h2 {
            color: #2e7d32;
            text-align: center;
        }
        p {
            font-size: 18px;
            color: #4e944f;
        }
    </style>
</head>
<body>
    <div class="profile-card">
        <h2>📦 Profil Vendor</h2>
        <p><strong>Nama Syarikat:</strong> <?= htmlspecialchars($user['nama_syarikat']) ?></p>
        <p><strong>No Syarikat:</strong> <?= htmlspecialchars($user['no_syarikat']) ?></p>
        <p><strong>Lokasi Kutipan:</strong> <?= htmlspecialchars($user['lokasi_kutipan']) ?></p>
        <p><strong>Jenis Barang Dikumpul:</strong> <?= htmlspecialchars($user['jenis_barang']) ?></p>
        <hr>
        <p><strong>Nama:</strong> <?= htmlspecialchars($user['nama']) ?></p>
        <p><strong>Email:</strong> <?= htmlspecialchars($user['email']) ?></p>
        <p><strong>No Telefon:</strong> <?= htmlspecialchars($user['telefon']) ?></p>
    </div>
</body>
</html>
